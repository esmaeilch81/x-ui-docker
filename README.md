---

````markdown
# 🐳 X-UI Ultimate Docker

**یک پنل قدرتمند و آماده از X-UI برای نصب آسان در Docker**

---

## ✨ ویژگی‌ها

- ⚙️ **نصب سریع با یک دستور**
- 🔐 **پشتیبانی از SSL خودکار**
- ♻️ **به‌روزرسانی و سلامت‌سنجی خودکار**
- 🧩 **مدیریت ساده از طریق CLI**
- 🚀 **پشتیبانی از چند پورت و تنظیمات پیشرفته**

---

## 🚀 نصب سریع

```bash
docker run -d --name x-ui -p 54321:54321 esmaeilch81/x-ui-ultimate:latest
````

🖥️ **آدرس پنل:**
`http://<آی‌پی-سرور-شما>:54321`

🔑 **نام کاربری:** `admin`
🔒 **رمز عبور:** `admin`

---

## ⚡ دستورات مدیریتی

```bash
# مشاهده وضعیت
docker exec x-ui x-ui status

# راه‌اندازی مجدد
docker exec x-ui x-ui restart

# مشاهده لاگ‌ها
docker exec x-ui x-ui log

# مشاهده تنظیمات
docker exec x-ui x-ui settings
```

---

## 🛠️ تنظیمات پیشرفته

### اجرای کامل با تمام پورت‌ها:

```bash
docker run -d --name x-ui \
  -p 54321:54321 \
  -p 80:80 \
  -p 443:443 \
  --restart unless-stopped \
  esmaeilch81/x-ui-ultimate:latest
```

### تغییر نام کاربری و رمز عبور:

```bash
docker exec x-ui x-ui setting -username myadmin -password mypass123
docker exec x-ui x-ui restart
```

---

## 📬 ارتباط با سازنده

* **📧 ایمیل:** [esich81@gmail.com](mailto:esich81@gmail.com)
* **💬 تلگرام:** [@esmaeilch81](https://t.me/esmaeilch81)
* **🐳 Docker Hub:** [esmaeilch81/x-ui-ultimate](https://hub.docker.com/r/esmaeilch81/x-ui-ultimate)

---

## 📄 لایسنس

**MIT License** – استفاده، تغییر و بازتوزیع آزاد است.

---

> 💡 فقط با یک دستور، X-UI کامل را روی سرور خود داشته باشید!
> سریع، امن، خودکار. 🎉

```

